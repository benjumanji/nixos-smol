# Explanation

Minimal non-trivial example to look at importing a third party module using npins + specialArgs.

# Eval

```
nixos-rebuild build-vm -f .
```

# Test 

run the vm and try `, cowsay hai`.
